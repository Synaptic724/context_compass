# Example: Artifact Workflow

1. Create artifact linked to active ticket.
2. Add artifact row in `artifact_board.md`.
3. Record evidence note in active ticket `## Notes`.
4. Apply ticket-close disposition (`delete_on_close` by default).
