# Example: tests_components

This example demonstrates a compliant structure for
`system_docs/tests_components.md`.

Expected signals
- C3 test components include lifecycle and failure-mode fields.
- C1 call flows reference concrete fixture/function names.
- Information sources map cleanly to promoted FACT claims.

Use this as a starter pattern.
